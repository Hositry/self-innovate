FUNC void Setup (void) 
{ 
SP = _RDWORD(0x800C800);          // 堆栈指针 
PC = _RDWORD(0x800C804);          // PC 
_WDWORD(0xE000ED08, 0x800C800);   // 中断向量偏移地址 
} 
load %L incremental
Setup();
g, main                             //跳转到main  
